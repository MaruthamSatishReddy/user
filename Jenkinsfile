@Library('easyToBuy-Jenkins_SharedLib') _
pipeline {
   agent any
   stages {
      stage('Git Checkout') {
         steps {
            branch: "master",
            url: "https://github.com/MaruthamSatishReddy/user.git"
         }
      }
  }
}

